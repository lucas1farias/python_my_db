

def dica():
    """
    Se um atributo for o mesmo (em comum), entre todos os objetos, então ele deve ser um atributo de classe
    Caso contrário, então deve ser um atributo de instância
    """

def ex_atrib_classe():
    """
    Relacionados a partes físicas do objeto, que é padrão em comum entre os objetos envolvidos

    ABORDAGEM:
              seres humanos possuem olhos em comum
              ventiladores possuem hélices em comum
              carros possuem rodas
    """

def ex_atrib_inst():
    """
    Relacionado a características de um objeto, que não é padrão em comum entre os objetos envolvidos

    ABORDAGEM:
              seres humanos possuem cor dos que não são próprias para todos
              ventiladores possuem formatos que não são pŕoprias para todos
              carros possuem preços que não são próprios para todos
    """
