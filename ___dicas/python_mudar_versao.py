

"""
    * Terminal
        py - 0           [Exibe as versões Python instaladas]
        python --version [Exibe a versão instalada/utilizada no momento]
        python -V        [Exibe a versão instalada/utilizada no momento]
        py -version_int  [Entra no console python referente a versão especificada (não troca o interpretador atual)]

    * Configurações
        Este computador -> bd -> Propriedades -> Configurações avançadas do sistema
        Variáveis de ambiente

    * NÃO DEU CERTO (preciso pesquisar)
        python --version
        alias python=python3.10
        python --version
"""
