

"""
Objetivo:
         listar rotas existentes no projeto (não entendo o propósito)
"""


def fonte():
    """
    Curso:  # Programação Web com Python e Django framework: Essencial
    Local:  # Seção 7:Dissecando o Django User Model
    Aula:   # 68. Login e Autenticação
    Minuto: # 04:33
    """


def terminal():
    """
    python manage.py shell
    from pp.settings import *
    from pprint import pprint
    """


# Comparação
def terminal2():
    """
    INSTALLED_APPS
    ['pa', 'bootstrap4', 'django.contrib.admin', 'django.contrib.auth', 'django.contrib.contenttypes', 'django.contrib.sessions', 'django.contrib.messages', 'django.contrib.staticfiles']

    pprint(INSTALLED_APPS)
    ['pa',
     'bootstrap4',
     'django.contrib.admin',
     'django.contrib.auth',
     'django.contrib.contenttypes',
     'django.contrib.sessions',
     'django.contrib.messages',
     'django.contrib.staticfiles']
    """
