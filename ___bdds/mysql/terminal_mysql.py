

def comandos():
    """
    Abrir o terminal
    Digitar: [ mysql -u lucasf1 -p ]           || logar
    Digitar: [ senha ]                         || logar
    Digitar: [ SHOW DATABASES; ]               || visualizar bancos
    Digitar: [ USE nome_do_bdd; ]              || selecionar banco
    Digitar: [ SHOW TABLES; ]                  || visualizar tabela(s) de um banco logado
    Digitar: [ DROP TABLE nome_da_tabela; ]    || excluir tabela de um banco
    Digitar: [ CREATE TABLE nome_da_tabela; ]  || criar tabela em um banco
    Digitar: [ SELECT * FROM nome_da_tabela; ] || mostrar o conteúdo inteiro de uma tabela em um banco
    Digitar: [ CREATE DATABASE nome_do_bdd; ]  || criar banco
    Digitar: [ DROP DATABASE nome_do_bdd; ]    || deletar banco
    """
