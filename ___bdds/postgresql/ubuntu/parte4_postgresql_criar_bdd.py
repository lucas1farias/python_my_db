

# Criação de um bdd dentro do servidor de um usuário 'não root'
def parte_1():
    """
    ----------------------------------------------------- CAMINHO -----------------------------------------------------
    • Servers (clicar) -> procurar pelo servidor alvo de um usuário (botão direito) -> Create -> Database

    --------------------------------------------------- CONFIGURAÇÃO ---------------------------------------------------
    • General || Database = inserir o nome do bdd (recomendação: nome do usuário não 'root' + _dtb + _int)
    • [ Save ]
    """
