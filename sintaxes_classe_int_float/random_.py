

"""
Objetivo:
    gerar uma classe flutuante menor que 1, normalmente para ser parte em iteráveis
"""

from random import random

print([1], random())
print([2], l := [random(), random(), random()])
