

def parte_1():
    """
    Os comandos a seguir servem para testar o funcionamento do PostgreSQL e verificar usuários já criados

    -------------------------------------------------- PROCEDIMENTOS --------------------------------------------------
    • sudo su - postgres  || entra no console com o usuário 'root'
    • psql                || acessar o console
    • \du                 || verificar a lista de usuários (apertar Q para sair)
    • sudo su - nome_user || logar em um usuário da lista
    """
